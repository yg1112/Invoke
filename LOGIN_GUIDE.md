# 🔐 Fetch App 登录指南

## 当前状态

✅ **API 服务器已启动** - 运行在 `http://127.0.0.1:3000`  
⚠️ **需要完成 Gemini 登录** - 才能使用 Chat Completions

---

## 📱 登录步骤

### 方法 1: 通过 App 窗口登录（推荐）

1. **找到 Fetch App**
   - 检查 Dock 栏是否有 Fetch 图标（小鸟图标 🐦）
   - 如果没有，在 Spotlight 搜索 "Invoke" 或 "Fetch"

2. **打开 App**
   - 点击 Dock 图标，或双击 App
   - 应该会看到一个浮动窗口

3. **点击 Login 按钮**
   - 在窗口中找到 "Login" 按钮
   - 点击后会弹出一个新的浏览器窗口

4. **完成 Google 登录**
   - 在浏览器窗口中输入你的 Google 账号
   - 完成验证（可能需要输入密码或验证码）

5. **等待登录完成**
   - 登录成功后，会听到 "叮" 一声
   - 浏览器窗口会自动关闭
   - 主窗口的状态会变为 "🟢 Connected"

6. **验证登录**
   ```bash
   cd /Users/yukungao/github/Fetch
   ./test_api_server.sh
   ```
   如果看到 "✅ Chat Completions 成功！"，说明登录完成。

---

### 方法 2: 通过 Cookie 登录（高级）

如果你已经在 Chrome 中登录了 Gemini，可以复制 Cookie：

1. **打开 Chrome**
   - 访问 `https://gemini.google.com/app`
   - 按 `F12` 打开开发者工具
   - 在 Console 中输入：`document.cookie`

2. **复制 Cookie 字符串**
   - 复制输出的所有 Cookie

3. **在 Fetch App 中使用**
   - 找到 "Cookie Login" 选项
   - 粘贴 Cookie 字符串
   - 点击确认

---

## 🧪 测试 API

登录完成后，运行测试脚本：

```bash
cd /Users/yukungao/github/Fetch
./test_api_server.sh
```

### 手动测试 Chat Completions

```bash
curl -X POST http://127.0.0.1:3000/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gemini-2.0-flash",
    "messages": [{"role": "user", "content": "Say hello"}]
  }'
```

### 测试 Aider 连接

```bash
# 进入一个测试项目目录
cd ~/Desktop/test_repo  # 或你的项目目录

# 连接 Fetch API
aider \
  --openai-api-base http://127.0.0.1:3000/v1 \
  --openai-api-key fetch-key \
  --model openai/gemini-2.0-flash \
  --no-git \
  --message "Write a hello world python script"
```

---

## 🐛 故障排除

### App 窗口找不到？

```bash
# 检查 App 是否在运行
ps aux | grep Invoke | grep -v grep

# 如果没运行，重新启动
cd /Users/yukungao/github/Fetch
.build/debug/Invoke &
```

### 端口被占用？

```bash
# 检查端口
lsof -i :3000

# 如果被占用，杀掉进程
kill -9 $(lsof -ti :3000)
```

### 登录后仍然报错？

1. 检查 Xcode 控制台（如果有）查看错误信息
2. 检查 `/tmp/fetch.log` 日志文件
3. 重新启动 App 并重试登录

---

## ✅ 成功标志

登录成功后，你应该看到：

- ✅ 主窗口状态显示 "🟢 Connected"
- ✅ `test_api_server.sh` 测试全部通过
- ✅ `curl` 测试返回 JSON 响应（不是错误）

---

## 📞 下一步

登录完成后，你可以：

1. **使用 Aider 进行代码编辑**
2. **通过 API 直接调用 Gemini**
3. **集成到其他工具中**

享受你的本地 AI 编程助手！🎉



